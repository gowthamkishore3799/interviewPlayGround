/**
 * Using boyer moore algorithm
 * 
 */


const boyerMoore = function(st1, str2) {


    const hashMap = {};


    for(let char of str2){
        // hashMap[char] = Math.max(1,str2.length-)
    }
}


console.log(boyerMoore("hello world", "hello"));