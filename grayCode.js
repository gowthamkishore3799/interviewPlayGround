var grayCode = function(n) {
    let noOfElements = (2 ** n) -1

    let result = [0];
    let val = 0;

    for (let i = 0; i < total; i++) {
        result.push(i ^ (i >> 1));
    }

    console.log(result)
};


console.log(grayCode(2))



safafs
faafss
